# MCP — delegated to agent-toolkit

MCP templates are distributed by [agent-toolkit](https://github.com/ulises-jeremias/agent-toolkit).

Install via:

```bash
dots-skills install-toolkit
agent-toolkit install
```

See `docs/MCP_TEMPLATES.md` and `docs/AGENT_TOOLKIT.md`.
