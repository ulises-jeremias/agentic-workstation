# Loops — delegated to agent-toolkit

Loop templates are distributed by [agent-toolkit](https://github.com/ulises-jeremias/agent-toolkit).

Install via:

```bash
uv tool install --force agent-toolkit-cli
agent-toolkit install
agent-toolkit loop list
```

See `docs/LOOPS.md` and `docs/AGENT_TOOLKIT.md`.
