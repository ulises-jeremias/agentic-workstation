company:
  name: agentic-workstation
paths:
  local_bin: "{{ .chezmoi.homeDir }}/.local/bin"
  dots_ai_share: "{{ .chezmoi.homeDir }}/.local/share/agentic-workstation"
defaults:
  shell: zsh
  editor: vi
features:
  ai: true
  mcp_templates: true
  workstation_doctor: true
